# Hoisting para monorepo - mejora compatibilidad con paquetes compartidos
# hoisting configuration removed - shamefully-hoist is not supported by npm

# Evita que los postinstall de las apps causen problemas al instalar desde la raíz
ignore-scripts=false

# Fuerza instalación física en node_modules en lugar de enlaces cuando sea posible
install-links=false
